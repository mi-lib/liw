PREFIX=$(HOME)/usr
PROJNAME=liw
VERSION=1.2.4

DEPENDENCY="zeda=1.5.10"

# dynamic linker (libdl)
CONFIG_USE_LIBDL=y

# POSIX thread (pthread)
CONFIG_USE_PTHREAD=y

# Realtime Extension
CONFIG_USE_RT=y

# Enhanced Realtime Controller (RTC)
CONFIG_USE_RTC=y
